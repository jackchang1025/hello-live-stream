---
alwaysApply: true
description: 优化的代码实施流程（TDD 优先、最小变更、质量门禁、可回滚）
---

# 代码实施流程（优化版）

本规则在整个仓库中全局生效，指导以“测试先行、最小可行变更、可验证交付”为核心的实施流程，适配本项目的技术栈与规范。

- 关联规范：PSR-12、严格类型、命名与类型提示、SOLID、依赖注入、无全局状态（参见工作区规则）
- 关键路径：后端（Hyperf 3）与前端（Vue 3）前后端分离，CI/质量门禁、文档与可回滚
- 参考路径：
  - 后端业务目录：[app/Baccarat/](mdc:app/Baccarat)
  - 依赖清单：[composer.json](mdc:composer.json)、[mine-ui/package.json](mdc:mine-ui/package.json)
  - CI 配置：[.github/workflows/](mdc:.github/workflows)
  - 环境配置：[.env](mdc:.env)、[.env.example](mdc:.env.example)
  - 容器编排：[docker-compose.yml](mdc:docker-compose.yml)

## 一、充分了解与分析现有代码（Discovery）

- 使用语义搜索从整体到局部定位：架构入口、模块边界、依赖关系、数据流、异常链路
- 明确“变更假设”“影响面”和“回滚点”
- 对数据库/缓存/队列/接口契约的改动，先产出“契约草案 + 最小迁移方案”
- 产出短文档（可附在 PR 描述）：目标、范围、不做什么、风险与缓解

在 Cursor 中的操作约定：
- 优先使用语义搜索定位（多组关键词，分模块并行搜索），必要时精确匹配
- 并行收集信息（多文件读取/搜索并发执行），减少串行等待
- 大文件优先跳转到函数/类签名阅读，再逐步展开

## 二、先编写测试（TDD）

- 测试优先级：公共 API → 关键业务路径 → 边界条件与异常 → 负载/性能（如需）
- 测试层次：
  - 单元测试：纯函数/领域服务/策略对象（隔离 IO）
  - 集成测试：数据库/缓存/消息队列/HTTP 客户端
  - 端到端（可选）：覆盖关键用户路径
- 断言策略：只断言业务必需行为；避免脆弱耦合内部实现
- 命名与组织：Given_When_Then 风格用例名；目录按模块镜像业务结构
- 工具：PHPUnit 或 Pest；测试数据用工厂/夹具；对外部系统打桩/替身

质量门槛（可在 CI 校验）：
- 关键模块测试覆盖率 ≥ 80%（至少语句/分支二选一）
- 失败用例为红，且能复现预期缺口

## 三、仅为通过失败测试实现最小代码（最小可行）

- 只实现让当前失败测试变绿所需的最小逻辑，避免超前设计
- 遵循项目规范：
  - 顶部 `declare(strict_types=1);`，严格类型
  - 完整参数与返回类型提示，属性类型提示
  - 依赖注入优先，避免在类内部直接创建依赖
  - 避免全局状态/静态工具；封装副作用并隔离边界
  - 错误处理：显式异常类型、清晰日志上下文（分类记录到 `runtime/logs/`）
- 提交原则（小步快跑）：
  - 每次提交只解决一个失败点或一个逻辑增量
  - 使用 Conventional Commits：`feat(scope): ...` / `fix(scope): ...` / `test(scope): ...` / `refactor(scope): ...`

## 四、重构与清理（保持测试全绿）

- 目标：在不改变对外行为的前提下，提升可读性、可维护性与性能
- 行动清单：消除重复、提取小函数、命名语义化、移除魔法数字/常量化、缩减嵌套、面向接口编程
- 数据与副作用隔离：将 IO 边界下沉至基础设施层，领域层保持纯净
- 性能关注：热点路径的算法复杂度与内存占用；必要时添加基准/粗粒度性能用例
- 安全检查：鉴权/鉴别、输入校验、异常泄露、幂等性

## 五、文档补全（与实现同步）

- API 文档：更新/生成 Swagger；标注请求/响应/错误码/示例
- 变更说明：更新 `CHANGELOG.md` 条目，记录兼容性影响与迁移步骤
- 运维说明：环境变量、配置位点、特性开关、回滚预案
- 开发文档：在对应模块目录添加/更新 `README` 或注释块（PHPDoc 标准）

## 六、Definition of Done（验收标准）

- 测试全绿；关键模块覆盖率达标
- 代码风格与静态分析通过（PHP-CS-Fixer/Laravel Pint、PHPStan）
- CI 全部检查通过（含构建、测试、质量、镜像/制品如适用）
- 无显著性能回退；核心指标未低于基线
- 文档已更新（API/运维/变更日志/迁移）
- PR 描述完整且自解释，附带风险与回滚说明

## 七、提交/PR 规范与回滚

- 分支：`feature/*`、`fix/*`、`refactor/*`
- 提交：小而频繁；信息可检索、语义明确
- PR 模板建议包含：
  - 变更目的/范围/不做内容
  - 影响面（接口/数据/配置/依赖）
  - 风险与缓解、回滚步骤
  - 测试说明（覆盖点/证明截图或日志）
- 回滚策略：
  - 代码层面：保证每个提交可安全回退
  - 数据层面：迁移具备可逆方案或保护脚本

## 八、在 Cursor 中的工作习惯（强制）

- 读代码前先列出信息需求，批量并行搜索与读取文件
- 在每轮操作前写简短状态更新；多文件/多搜索并行执行
- 大改动前先建立 TODO（原子任务、动词开头），逐项推进
- 任何代码编辑后：
  - 立即本地构建/测试并修复
  - 若出现 Linter 报错，最多尝试修复 2 轮；第 3 轮需人工决策
- 展示代码片段时遵循“引用已存在代码/提议新代码”的统一规范

## 九、常用命令与检查点

- 后端测试：`vendor/bin/pest`
- 代码风格：`./vendor/bin/pint` 或 PHP-CS-Fixer 预设
- 静态分析：`vendor/bin/phpstan analyse`
- 前端构建/检查：`npm run build`、`npm run lint`（见 [mine-ui/package.json](mdc:mine-ui/package.json)）

## 十、最小交付清单（每次改动）

- 失败测试 → 通过测试 → 重构后仍通过
- 小步提交，符合 Conventional Commits
- CI 绿灯；质量门禁达标
- 文档齐全；可回滚说明明确